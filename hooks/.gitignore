# This directory is for future hooks files.
# Delete this .gitignore file if you have added hooks files, so that git can track them

# For now ignore everything in this directory
*
# Except this file
!.gitignore
